---
- name: install vim and mlocate and htop git
  yum:
    name: "{{ item }}"
      item:
        - git
        - vim
        - mlocate
        - htop
    state: latest
  become: yes
- name: yum cleanup
  yum:
    autoremove: yes
  become: yes
