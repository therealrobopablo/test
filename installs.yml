---
  - name: install vim and mlocate and htop git
    yum:
    name: "{{ items }}"
      items:
      - git
      - vim
      - mlocate
      - htop
    state: latest
  - name: yum cleanup
    yum:
      autoremove: yes
