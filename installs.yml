---
  - name: install vim and mlocate and htop git
    yum:
      name: "{{items}}"
        with_items:
          - git
          - vim
          - mlocate
          - htop
        state: latest
  - name: yum cleanup
    yum:
      autoremove: yes
