---
  - name: install vim and mlocate and htop git
    yum:
    name: "{{items}}"
    with_items:
    - git
    - vim
    - mlocate
    - htop
    state: latest
    notify: updatedb
  - name: yum cleanup
    yum:
    autoremove: yes
handlers:
  - name: updatedb
    shell: updatedb
